## Routing

- react-router-dom
- useNavigate() hook to navigate btween diff pages; 
- can use only inside < RrowserRouter > { navigate('/dashboard')} < RrowserRouter />

### Lazy loading
- when page 1; give bundle of pg 1 only, if 2; for 2nd page only
- incremently give the bundle; radher than giving whole bundle at once; 

## Prop drilling

## Context-API
- helps to teleport the prop to child; without passing in every level;